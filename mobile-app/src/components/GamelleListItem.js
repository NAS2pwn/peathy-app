import { View } from "react-native";

export default function GamelleListItem({
    onPress,
    gamelle,
}) {
    return (
        <Pressable onPress={onPress}>
            /*TODO : design du clic pour la gamelle
            , faut un indicateur de si c'est connecté
            (genre un point rouge ou vert à droite)*/
        </Pressable>
    )
}
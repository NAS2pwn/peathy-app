import { View } from "react-native";

export default function GamelleListItem({
    onPress,
    pet,
}) {
    return (
        <Pressable onPress={onPress}>
            /*TODO : design du clic pour les animaux
            , faut une image chien ou chat à gauche*/
        </Pressable>
    )